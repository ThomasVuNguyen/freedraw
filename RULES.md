# Sync

- When a new item is created/edited/deleted - sync immediately to the database (500ms debounced)
- User's canvas pulls in up-to-date data from the database about the canvas every 2s

# Permission

- A user should not be able to edit, move, or delete another's user item, unless given admin privilleges

